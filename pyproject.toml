[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "activelog-pages"
version = "0.1.0"
description = "Log visualization and dashboard page builder"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
